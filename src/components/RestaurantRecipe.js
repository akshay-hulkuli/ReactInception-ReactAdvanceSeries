const RestaurantRecipe = (props) => {  
    return (
        <div className="res-recipe-accordian">
            <h2>{props?.recipeInfo?.card?.card?.title}</h2>
            
        </div>
    )
}

export default RestaurantRecipe;